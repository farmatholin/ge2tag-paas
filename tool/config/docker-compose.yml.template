geo2tag:
  image: %image_container%
  ports:
    - "%port%:80"
   #volumes:
    #- %user_dir%/plugin:/var/www/geomongo/src/plugins
    # add logs here
  #memory control here;
  links:
    - mongo
mongo:
  image: mongo:latest
  volumes:
    - %user_dir%/db:/data/db